# Plans

Implementation plans for active features. Each plan is a markdown file that tracks steps, decisions, and progress.

Plans persist across sessions — if context is compacted or you start a new session, just say "read plans/[name].md and continue."

Completed plans are moved to `plans/completed/`.
